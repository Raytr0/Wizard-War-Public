using UnityEngine;

public class GameManger : MonoBehaviour
{
    public Character deMorgan;
    public Enemy enemyPrefab;

    void Start()
    {
        //should set up main character and set the camera too
        /*Attack[] attackList = new Attack[1];
        Camera camera = Camera.main;
        Character mainChar = new Character(camera, 100, 0.08f, 20, 0, 0, attackList);*/
       // Enemy meleeEnemy = Instantiate(enemyPrefab);
        //meleeEnemy.Health(50);
        //meleeEnemy.Speed(5);

        //Enemy projEnemy = Instantiate(enemyPrefab);
        //projEnemy.Health(75);
        //projEnemy.Speed(7);

        //Enemy finalEnemy = Instantiate(enemyPrefab);
        //finalEnemy.Health(150);
        //finalEnemy.Speed(10);
    }
}